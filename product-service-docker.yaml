server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://postgres-server:5432/ecoms_product
    username: erp
    password: wein123!
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true