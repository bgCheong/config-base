server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ecoms_user
    username: erp
    password: wein123!
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true